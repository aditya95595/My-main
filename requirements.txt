# Core computer vision and ML
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
easyocr>=1.7.0

# Optional: for GUI on a desktop / with Xvfb
# tkinter is part of the standard library on most systems

# Optional keyboard hotkey (only works when a real keyboard is available)
# keyboard>=0.13.5

# Standard library used:
# - logging, os, sys, threading, time, queue, subprocess, dataclasses
